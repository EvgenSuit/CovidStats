# covidstats

This is the source code of the Flutter app that allows anyone with Android and IOS devices to detect if a person is wearing a mask and get the most up-to-data covid data in a pretty representation.

The release version of the app called "app-release.apk" is provided

Optimization updates to come.
